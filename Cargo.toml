[package]
name = "folio"
version = "0.2.1"
edition = "2024"
description = "A small, Omarchy-native PDF reader and editor"
license = "MIT"

[dependencies]
anyhow = "1.0"
dirs = "6.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tempfile = "3.23"

[profile.release]
lto = "thin"
strip = true
